# Pixel Snake Lua template
Standalone script game example requiring explicit experimental `vqeaf_lua_beta`.
Press START to begin/pause; D-pad to turn; B exits through firmware. No direct
SD/network/file permissions. The host preview is not the real device image.
